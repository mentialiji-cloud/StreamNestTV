<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Many IPTV providers still use HTTP. Remove cleartext support if your provider uses HTTPS only. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
